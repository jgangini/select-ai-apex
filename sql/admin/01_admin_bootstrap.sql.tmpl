-- Rendered by select-ai-apex.
-- Creates/reuses the parsing schema, grants required Select AI/APEX privileges,
-- grants object-level SELECT access, and creates/reuses the APEX workspace.
--
-- Generate a concrete script with:
--   select-ai-apex plan --mode existing --oci-config .oci/config --oci-key .oci/key.pem --wallet wallet.zip --schemas HR
